
alias ls="ls --color=auto"
alias pypy=/home/ronny/Projects/pypy-trunk/pypy/translator/goal/pypy-c

SUDO_ALIASES=(
    modprobe insmod rmmod
    apt-get
)

for com in ${SUDO_ALIASES[@]}
do
    alias $com="sudo $com"
done

